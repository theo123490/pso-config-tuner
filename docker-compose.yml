services:
  controller:
    build: .
    ports:
      - "8080:8080"
    environment:
      - CONTROLLER_ADDR=:8080
      - REDIS_ADDR=redis:6379
    volumes:
      - ./configs:/app/configs
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
